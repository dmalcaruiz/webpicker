name: learning
description: "CHL Color Picker"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cyclop: ^0.7.0
  snapping_sheet_2: ^3.1.5
  reorderable_grid_view: ^2.2.8
  image: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
